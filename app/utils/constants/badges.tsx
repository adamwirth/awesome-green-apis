import { Badge } from "@aws-amplify/ui-react";

export const AlphaBadge = () => (
    <Badge
        size="small"
        variation="warning"
        style={{'cursor': 'progress'}}>
        alpha
    </Badge>
);